import "./newLabel.scss";

import newLabel from "../../resources/icons/new.png";

const NewLabel = () => {
  return (
    <div className="newLabel">
      <img src={newLabel} alt="newLabel" />
    </div>
  );
};

export default NewLabel;
